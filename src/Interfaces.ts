// Interfaces.ts
export interface ApiFunctionDetails {
  functionName: string;
  method: string;
  url: string;
  // Add other properties as needed
}

export interface ErrorHandlingDetails {
  errorTypes: string[]
  // Other properties...
}
